tasks:
  - name: Dependencies & Dev Server
    init: nvm install 14.08 | && nvm use 14.08 | && nvm alias default 14.08 | npm ci | npm start

# Ports to expose on workspace startup
ports:
  - port: 3000
    onOpen: open-preview

# VSCode Extensions
vscode:
  extensions:
    - vsls-contrib.codetour

# image:
#   file: .gitpod.Dockerfile